// const sliderItems = document.querySelectorAll('.ingridients_list--item');
// [...sliderItems].forEach((slide) => {
//   replicate(slide);
// });

// function replicate(slide) {
//   const slider = document.querySelector('.ingridients_list');
//   const parentWidth = slider.getBoundingClientRect().width,
//     itemsWidth = slide.getBoundingClientRect().width;
//   var currentWidth = itemsWidth;
//   console.log(currentWidth);
//   console.log(parentWidth);
//   while (currentWidth < parentWidth + itemsWidth) {
//     let clonedItems = slide.cloneNode(true);
//     slider.appendChild(clonedItems);
//     currentWidth = currentWidth + itemsWidth;
//   }
//   // slider.classList.add('slide');
// }
